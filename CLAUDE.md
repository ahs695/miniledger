# miniledger

Microservices ledger app: `account-service` (accounts) and `transaction-service` (transactions), Node.js + Express.

## Structure
```
miniledger/
├── docker-compose.yml       # postgres + mongo + both services, orchestrated (see Status log for the env override details) — the actual way this project runs, see Working agreement
├── package.json              # root-only — just the "docker:up" script (docker compose up --build)
├── DEPLOYMENT.md              # ECS Fargate design doc — reference only, explicitly deferred indefinitely (see Status log / Working agreement)
├── dashboard/index.html       # static demo UI, inline style+script, no build tooling — talks directly to both services (needs cors)
├── .gitignore                # ignores node_modules/, .env (matches at any depth)
├── account-service/
│   ├── Dockerfile             # multi-stage: builder (npm ci --omit=dev) → slim final stage, non-root appuser, EXPOSE 4001
│   ├── .dockerignore          # node_modules, .env, *.test.js, .git
│   ├── jest.config.js         # globalSetup (resets+migrates DATABASE_URL_TEST once) + setupFiles (points DATABASE_URL at it per test file)
│   ├── test/globalSetup.js    # DROP SCHEMA CASCADE + re-run all migrations — DATABASE_URL_TEST only, hard-fails if unset
│   ├── test/setupEnv.js       # process.env.DATABASE_URL = process.env.DATABASE_URL_TEST for every test file
│   ├── src/index.js           # thin entry point: dotenv, validateEnv, require("./app"), app.listen
│   ├── src/app.js             # the actual Express app (no listen) — what tests require
│   ├── src/db.js              # pg Pool + query() helper, logs slow queries (>100ms) at warn
│   ├── src/repositories/accountRepo.js # createUser (txn: user+wallet), getWalletByUserId, adjustBalance
│   ├── src/repositories/accountRepo.test.js # jest, pg fully mocked — 4 tests on adjustBalance, no real DB needed
│   ├── src/middleware/auth.js # requireAuth — verifies Bearer JWT against JWT_SECRET, sets req.user
│   ├── src/routes/auth.js     # POST /auth/token — dev-only (NODE_ENV !== "production"), mints a 1h token
│   ├── src/routes/users.js    # POST /users
│   ├── src/routes/users.test.js # supertest against real app.js + real DATABASE_URL_TEST Postgres
│   ├── src/routes/wallets.js  # GET /wallets/:userId (requireAuth), GET /internal/wallets/:userId (open), POST /internal/wallets/:walletId/adjust (open)
│   ├── src/routes/wallets.test.js # supertest — 200/404/401, token minted via real POST /auth/token
│   ├── src/scripts/migrate.js # runs ALL migrations/*.sql (sorted) against DATABASE_URL — not idempotent, see note below
│   ├── migrations/001_init.sql # users, wallets tables
│   ├── migrations/002_idempotency.sql # idempotency_keys table
│   ├── .env                  # untracked, local values (now also DATABASE_URL_TEST)
│   └── .env.example          # committed template
└── transaction-service/
    ├── Dockerfile              # same multi-stage pattern as account-service, EXPOSE 4002
    ├── .dockerignore           # node_modules, .env, *.test.js, .git
    ├── src/index.js           # thin entry point: dotenv, validateEnv, connectDB, require("./app"), app.listen
    ├── src/app.js             # the actual Express app (no listen, no connectDB) — what tests require
    ├── src/db.js              # mongoose.connect + Transaction.init() (waits for indexes) + Windows SRV-DNS workaround, blocks startup on failure
    ├── src/models/Transaction.js
    ├── src/repositories/transactionRepo.js # createPending, markCompleted, markFailed, findByUser
    ├── src/clients/accountClient.js # getWallet, adjustWallet — axios instance w/ axios-retry (3x exponential backoff, never retries 422)
    ├── src/routes/transfer.js # POST /transfer — orchestrates the debit+credit across account-service
    ├── src/routes/transfer.test.js # supertest, accountClient jest.mock'd (no real HTTP), real MongoDB Atlas — cleans up its own docs
    ├── src/routes/transactions.js # GET /transactions/:userId — paginated history via transactionRepo.findByUser
    ├── .env
    └── .env.example
```

## Services

| Service | Default port | Required env vars |
|---|---|---|
| account-service | 4001 | `PORT`, `DATABASE_URL`, `JWT_SECRET` |
| transaction-service | 4002 | `PORT`, `MONGO_URI`, `JWT_SECRET`, `ACCOUNT_SERVICE_URL` |

Both services: `require("dotenv").config()` runs first, then a `validateEnv()` check throws + `process.exit(1)` if any required var is missing. On successful start, one winston `info` line logs the port and `NODE_ENV`. Both expose `GET /health` → `{ status: "ok", service: "<name>" }`.

Run dev server: `npm run dev` (nodemon) inside each service folder. Both `index.js` files are thin now — they `require("./app")` (the real Express app, no `listen`) and only add env validation / `listen` / (transaction-service) `connectDB`. `npm test` (jest) works in both services now.

### account-service API
- `POST /users` — body `{ email, initialBalance? }` (email must contain `@`; `initialBalance` optional, defaults to `0`, must be a non-negative number if given — `400` otherwise). `201 { userId, walletId, balance, currency }` (balance is a string, e.g. `"0.00"` — Postgres `numeric` via `pg`, not converted to `Number`). `409 { error: "email already in use" }` on duplicate email. Creates the user + wallet atomically (single DB transaction in `accountRepo.createUser`) at the given starting balance — not represented as a `Transaction` in transaction-service (no corresponding record), it's just how the wallet starts out.
- `DELETE /users/:userId` — `200 { userId, deleted: true }` on success. `404 { error: "user not found" }` for an unknown user. `409 { error: "cannot delete user with a non-zero wallet balance", balance }` if the wallet isn't exactly zero — deliberate: silently deleting a user with money still in their wallet would destroy ledger data, so deletion requires zeroing the balance first (e.g. via `/internal/wallets/:walletId/adjust`). Locks the wallet row (`FOR UPDATE`) while checking, same race-prevention reasoning as `adjustBalance`. No auth, matching `POST /users`' openness (not `GET /wallets/:userId`'s auth) — consistent with the rest of the deliberately-open CRUD routes, and lets the dashboard call it directly.
- `GET /wallets/:userId` — **requires auth** (`Authorization: Bearer <jwt>`, 401 `{error:"missing token"}` / `{error:"invalid or expired token"}` on failure; `req.user` set to the decoded payload). `200 { userId, walletId, balance, currency }`, or `404 { error: "user not found" }`.
- `GET /internal/wallets/:userId` — same response shape as above, **no auth**. Added specifically for transaction-service's `accountClient.getWallet()` (service-to-service lookups) — the authenticated route stays as the one for real end-user access. Both share the same handler logic (`sendWalletLookup` helper in `wallets.js`).
- `POST /auth/token` (dev-only, disabled when `NODE_ENV === "production"`) — body `{ userId }` → `{ token }` signed with `JWT_SECRET`, 1h expiry. Exists purely to test protected routes locally; not a real login flow (no password/credential check).
- `POST /internal/wallets/:walletId/adjust` — body `{ amount: number > 0, type: "debit"|"credit" }` + required `Idempotency-Key` header. `400` if the header or a valid body field is missing. `200 { walletId, balance, currency }` on success. `422 { error: "insufficient funds" }` if a debit would take the balance below zero (nothing written). `404 { error: "wallet not found" }` for an unknown walletId. Replaying the same `Idempotency-Key` returns the original cached response (status + body) without re-applying the adjustment — implemented in `accountRepo.adjustBalance` via one DB transaction: check `idempotency_keys` first, then `SELECT ... FOR UPDATE` on the wallet row (see in-code comment on why the lock is needed to prevent a lost-update race between concurrent adjustments), compute/check/update, then insert the idempotency row, all before `COMMIT`.
  - Known gap (not addressed): two *simultaneous* requests with the same brand-new `Idempotency-Key` aren't fully guarded — both could pass the initial "not found" check before either commits. The second would fail at INSERT (`idempotency_keys` unique violation) and get a 500 rather than the cached response. Worth a follow-up if concurrent retries with the same key are expected.

### transaction-service data layer
- `Transaction` model (Mongoose): `fromWalletId`, `toWalletId` (String, required), `amount` (Number, required, min 0.01), `status` (enum pending/completed/failed, default pending), `idempotencyKey` (String, required, unique), `metadata` (Mixed, default `{}`), `completedAt` (Date), plus `{ timestamps: true }` → real `createdAt`/`updatedAt` (added later, see decisions log — docs created before that change have no `createdAt` and sort last under `{createdAt:-1}`).
- `transactionRepo`: `createPending({fromWalletId, toWalletId, amount, idempotencyKey})`, `markCompleted(id)`, `markFailed(id, reason)` (reason stored at `metadata.reason`), `findByUser(walletId, {limit=20, offset=0})` (matches `fromWalletId` OR `toWalletId`, sorted `{createdAt:-1}`). `createPending` does **not** catch duplicate-key errors itself — a repeated `idempotencyKey` throws a raw Mongo `E11000` (code `11000`) for whatever caller needs to handle it (currently nothing catches it explicitly — `POST /transfer` always generates a fresh `uuidv4()` per request so it never collides in practice).
- `accountClient.js`: uses a dedicated `axios.create()` instance (not the bare `axios` export) with `axios-retry` configured — `retries: 3`, `retryDelay: axiosRetry.exponentialDelay`, custom `retryCondition`: never retries a `422` (real business failure — insufficient funds), otherwise retries network errors or `5xx`. Applies to both `getWallet` (GET, naturally safe to retry) and `adjustWallet` (POST — safe to retry specifically *because* it always carries the caller's `Idempotency-Key`, so a retry either re-runs the same op or hits account-service's idempotency cache, never double-applies). `require("axios-retry").default` is the correct CJS import for the installed v4.5.0 (verified — the package is primarily ESM with a `.default` CJS interop). `getWallet(userId)` (GET `/internal/wallets/:userId`, `404` → `null`, anything else rethrows) and `adjustWallet(walletId, amount, type, idempotencyKey)` (POST `/internal/wallets/:walletId/adjust` with `Idempotency-Key` header, throws on any non-2xx).
- `GET /transactions/:userId` (`src/routes/transactions.js`) — `?limit` (default 20) / `?offset` (default 0), must be non-negative integers or `400`. `getWallet(userId)` → `404 { error: "user not found" }` if missing, else `200` with `findByUser(wallet.walletId, {limit, offset})`, newest first.
- `POST /transfer` — body `{ fromUserId, toUserId, amount }`. Validates `amount >= 0.01` (matches the schema's own `min`, not just `> 0`) and `fromUserId !== toUserId`, both non-empty strings → `400` otherwise. Looks up both wallets (`404 { error: "user not found" }` if either is missing) → creates a `pending` Transaction → debits the source then credits the destination, **using two sub-keys derived from one generated idempotencyKey** (`${key}:debit` / `${key}:credit` — see the fix noted below) → on success `markCompleted` + `201` with the transaction; on any failure from the debit/credit pair, `markFailed(id, message)` (message prefers account-service's `{error}` body, falls back to `err.message`) + `502` with the (failed) transaction. Unexpected errors elsewhere in the handler → `500`.
  - **Known, intentional gap:** if the debit succeeds but the credit fails, there's no compensating reversal — the transaction is marked `failed` and the caller gets `502`, but the source wallet stays debited. No saga/outbox pattern exists yet; worth a follow-up if this needs to be production-safe.
- **Bug found + fixed before implementing:** the task as originally described called for reusing one `idempotencyKey` for both the debit and credit calls. account-service's `idempotency_keys` table is keyed globally (not per-wallet), so the credit call would have found the debit's already-cached response and replayed it — silently never crediting the destination wallet, even though the transfer would report `201` success. Confirmed by design review before writing any code (user agreed); fixed by deriving `${idempotencyKey}:debit` / `${idempotencyKey}:credit`. Verified via curl: a real transfer moved money out of the source wallet **and** into the destination wallet correctly (125.00/75.00 after a 75 transfer on a 200/0 starting split) — this is exactly the check that would have caught the bug if the fix hadn't been applied.
- **Second issue found + fixed:** account-service's `GET /wallets/:userId` requires a Bearer JWT (added in an earlier task), which `accountClient.getWallet()` has no way to supply. Resolved (user's choice) by adding an unauthenticated `GET /internal/wallets/:userId` to account-service for service-to-service use, leaving the original route's auth intact for end-user access.
- **Bug found + fixed during testing:** `mongoose.connect()` resolves before background index builds (e.g. the unique `idempotencyKey` index) finish — confirmed via a real race: a duplicate-key insert immediately after connecting succeeded (should have failed) because the unique index didn't exist yet. Fixed by having `connectDB()` in `src/db.js` also `await Transaction.init()` (resolves once that model's indexes are confirmed built) before returning — so `index.js`'s `app.listen` never starts until the unique constraint is actually enforceable. If more models are added later, each will need the same `.init()` treatment (or switch to `mongoose.connection.syncIndexes()` for all registered models) — currently only `Transaction` is covered.
- **DB backend: MongoDB Atlas** (switched from the local `mongod`, see below). `MONGO_URI` in `.env` is a real `mongodb+srv://` connection string with live credentials (username `siddiquiahs1_db_user`, cluster `miniledger.nyjtzhh.mongodb.net`) — `.env` is gitignored, `.env.example` has a placeholder only. **Second bug found + fixed:** on this machine, Node's DNS resolver (c-ares) is configured to query `127.0.0.1`, which refuses raw SRV queries even though normal OS-level DNS resolution works fine (verified via `Resolve-DnsName` in PowerShell, which succeeded) — a known Windows+Node+Atlas quirk. This made `mongodb+srv://` lookups fail with `querySrv ECONNREFUSED` even with a correct connection string and correct Atlas Network Access rules. Fixed in `src/db.js` by calling `dns.setServers(["8.8.8.8", "1.1.1.1"])` before connecting, whenever `MONGO_URI` starts with `mongodb+srv://`. If this service is ever run on a different machine/network where the default resolver works fine for SRV lookups, this is harmless (just an extra explicit resolver).

## Status / decisions log
- 2026-09-19: Added `initialBalance` (optional, on `POST /users`) and `DELETE /users/:userId` to account-service, per user request ("otherwise all will have 0 hence no transactions possible" + "give delete user functionality"). Deletion refuses non-zero-balance wallets (`409`) rather than silently destroying ledger money — a deliberate design choice, not literally requested but treated the same as other correctness guards this project already has (idempotency, row locking). Added 5 new tests (`users.test.js`: funded creation, negative-balance rejection, delete-zero-balance success, delete-non-zero-balance refusal, delete-unknown-user 404) — 14/14 passing. Updated the dashboard: "Initial balance" field on the create-user form, a delete button (with balance-guard error surfaced inline) next to each list entry, and the Overview tab's endpoint reference. Verified live end-to-end via curl, including confirming the guard actually lifts once a wallet is debited back to zero (not a permanent block).
- 2026-09-18: Redesigned `dashboard/index.html` ("very pretty and dynamic" + explain project structure) — still one static file, zero dependencies (no CDNs, no build step), just far more visual polish: two tabs (**Overview** — SVG architecture diagram showing Dashboard→both services→their DBs plus the transaction-service→account-service internal-call arrow, endpoint reference cards, and a collapsible `<details>`-based file tree of the real repo structure; **Live Demo** — the original 3 functional sections, now with a session stats row, per-service live health-check dots polling `/health` every 5s, loading spinners on buttons, animated list/table entries, and copy-to-clipboard on created userIds), plus a light/dark theme toggle (persisted to `localStorage`, defaults to system preference). Fixed a real CSS bug caught during review before shipping: had nested an `@media` query inside a `:root[...]` selector block (invalid/unreliable), corrected to the standard pattern (`@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) {...} }` plus a separate explicit `:root[data-theme="dark"]` block for the manual toggle). All functional requirements from the original build preserved exactly: hardcoded URL constants, red-bordered error boxes with raw messages, plain HTML table for history. Verified: HTML tag balance checked (div/details/section/ul/svg all matched), re-served via the same `python -m http.server 8080` and confirmed the live page now serves the new markup. Still not clicked through in a real browser (Chrome extension declined) — verified structurally and via the same curl-based functional checks as the previous version, not eyeballed live.
- 2026-09-18: Added `dashboard/index.html` — a static, no-build demo UI (inline `<style>`/`<script>`, hardcoded `ACCOUNT_SERVICE_URL`/`TRANSACTION_SERVICE_URL` constants) with three sections: create user (`POST /users`, appends to an on-page list), transfer (`POST /transfer`, renders the returned transaction inline for *both* the 201-completed and 502-failed cases — not just success, since both carry a real transaction body worth showing), and transaction history (`GET /transactions/:userId`, rendered as a plain table). Errors (network failures, non-2xx with no meaningful body) show in a red-bordered box with the raw message. Found the actual "missing service" piece before building this: **neither service had CORS enabled** — a browser page fetching cross-origin (file:// or any static server) would be silently blocked on the preflight, regardless of correct JSON. Added `cors` (allow-all — documented as a local/portfolio-only tradeoff needing a real origin allowlist before any real exposure) to both `app.js` files. Verified for real: `npm test` still 9/9 + 2/2 after the CORS change; curled an actual `OPTIONS` preflight with an `Origin: null` header (matching what a `file://`-opened page sends) against both `POST /users` and `POST /transfer` and confirmed `Access-Control-Allow-Origin: *` comes back; then ran the exact three flows the dashboard performs (create two users, fund one out-of-band, transfer success + a deliberate insufficient-funds failure, load history) via curl and confirmed every response shape matches exactly what the dashboard's JS reads (`userId`/`walletId`/`balance`/`currency`; `_id`/`status`/`amount`/`metadata.reason`; the transactions array's `createdAt`/`fromWalletId`/`toWalletId`/`amount`/`status`). Could not click through it in an actual browser — the `claude-in-chrome` extension was declined — so this is curl-verified end-to-end but not eyeballed live; left both services running afterward for the user to do that manually.
- 2026-09-18: Wrote `DEPLOYMENT.md` (ECS Fargate design: two 256/512 task defs, Secrets Manager for `DATABASE_URL`/`MONGO_URI`/`JWT_SECRET`, ALB + `/accounts/*`/`/transactions/*` path routing, security groups) — flagged a real gap in that design: the services' actual routes aren't prefixed (`POST /users`, not `/accounts/users`), and ALB path rules don't rewrite paths, so the routing scheme as described would 404 until either the routes gain prefixes or something in front does the rewrite. Then attempted the ECR push (create 2 repos, push Phase-4 images): installed the AWS CLI via winget (no AWS account access existed on this machine), but had no credentials to configure — correctly declined to ask the user to paste an AWS access key/secret into chat (higher stakes than the Atlas password earlier: can create/delete/spend across the whole account) and instead walked them through generating IAM credentials themselves (scoped to `AmazonEC2ContainerRegistryFullAccess`, not admin) and running `aws configure` in their own shell. **User then decided to skip AWS entirely** ("do everything locally") — the ECR push was never completed, no AWS resources exist. `DEPLOYMENT.md` stays as a reference design doc but is explicitly deferred indefinitely, not a near-term next step; the project's actual "deployment" is `npm run docker:up` (local docker-compose, already working end-to-end as of the Phase 5 entry below). The AWS CLI installed via winget was left in place (harmless, no credentials configured) rather than uninstalled, since removing it wasn't asked for either.
- 2026-09-18: Added multi-stage `Dockerfile` + `.dockerignore` to both services (builder stage: `node:20-alpine`, `npm ci --omit=dev`, full source copy; final stage: `node:20-alpine`, non-root `appuser`/`appgroup`, only `node_modules`+`src` copied over with `--chown`, `EXPOSE`, `CMD ["node","src/index.js"]`). Built both standalone (`docker build -t miniledger-account ./account-service`, `-t miniledger-transaction ./transaction-service`) — confirmed non-root user via `docker run ... whoami` → `appuser`, and `docker images` sizes (~50MB/~52MB content size each on top of the shared alpine+node base layers). Since `.env` is excluded by `.dockerignore`, these images never contain one — env vars must be injected at run time (`-e`/`--env-file`/compose `environment:`), which is correct/standard but meant the images alone weren't runnable without help from something providing env vars (resolved by the docker-compose work below).
- 2026-09-18: Rewrote root `docker-compose.yml` into a real 4-service stack (`postgres`, `mongo`, `account-service`, `transaction-service`) and added a root `package.json` with just a `"docker:up": "docker compose up --build"` script (chose npm over a Makefile specifically because the requested name `docker:up` is natural npm-script syntax but awkward/fragile as a literal Make target name, since `:` is Make's target/prerequisite separator). Three fixes were necessary beyond the literal ask, all logged here since they're the difference between "looks right" and "actually works": (1) `env_file: ./<service>/.env` alone doesn't work inside the container network — account-service's `.env` has `DATABASE_URL` pointing at `localhost` (meaning the container itself, not the `postgres` service) and transaction-service's `.env` has a **real Atlas** `MONGO_URI` plus `ACCOUNT_SERVICE_URL=http://localhost:4001` (would leave the new `mongo` service unused and account-service unreachable) — fixed by keeping `env_file:` and adding a small `environment:` override block per service for just `DATABASE_URL` / `MONGO_URI` / `ACCOUNT_SERVICE_URL`, pointed at the in-network service names (`postgres`, `mongo`, `account-service`); (2) `postgres:16` refuses to boot with only `POSTGRES_DB` set (needs `POSTGRES_PASSWORD` or an explicit trust auth method) — added `POSTGRES_USER=postgres`/`POSTGRES_PASSWORD=postgres`, matching what `DATABASE_URL` already assumed; (3) dropped the obsolete top-level `version: "3.8"` key (modern `docker compose` just warns on it now) — pure cleanup. `account-service`'s `depends_on: postgres` uses `condition: service_healthy` against a `pg_isready` healthcheck (as asked); `transaction-service`'s `depends_on: [account-service, mongo]` is plain/un-gated (matching what was literally asked — only the postgres relationship was specified as health-gated) — flagged as a theoretical startup-race risk beforehand, but verified in practice it connected cleanly with no race. Ran `npm run docker:up` for real: Postgres ran both mounted migration files on first boot in order (`001_init.sql` then `002_idempotency.sql`, confirmed via container logs — `CREATE EXTENSION`/`CREATE TABLE` x3), transaction-service connected to the local `mongo` container and its `Transaction.init()` index-wait logic built the unique `idempotencyKey` index correctly there too, and **both `/health` endpoints responded `200` through the container stack** (curled from the host against `localhost:4001`/`4002`). Brought the stack down afterward (`docker compose down`) to avoid leaving a second Postgres/Mongo pair running alongside the pre-existing standalone `miniledger-postgres` container — named volumes (`postgres_data`, `mongo_data`) persist for next time regardless.
- 2026-09-18: Repo scaffolded (both services, package.json, dev scripts, health checks).
- 2026-09-18: Added `.env`/`.env.example` + env validation + winston startup log to both services.
- 2026-09-18: Added Postgres wiring to account-service (`pg`, `src/db.js`, `migrations/001_init.sql`, `src/scripts/migrate.js`, `npm run migrate`). Migration run successfully against a local Postgres — `users` and `wallets` tables confirmed via `\dt`.
- 2026-09-18: Added `POST /users` + `GET /wallets/:userId` (atomic user+wallet creation, tested via curl — 201/409/200/400/404 all verified against the live container).
- 2026-09-18: Added `migrations/002_idempotency.sql` (`idempotency_keys` table) and `POST /internal/wallets/:walletId/adjust` (idempotent, transactional, row-locked balance adjustment). Applied 002 directly via `docker exec ... psql` (not via `npm run migrate`, since that script now loops over all `migrations/*.sql` files with no applied-migrations tracking — re-running it against this container would try to recreate `users`/`wallets` from 001 again and fail; only safe to run end-to-end against a fresh database). Verified via curl: missing header/body → 400, debit past zero → 422 (confirmed nothing written to `idempotency_keys`), successful credit/debit update balance exactly (69.50 after 100 credit − 30.50 debit, no float drift), replaying the same `Idempotency-Key` returned the identical cached response without double-applying the adjustment, unknown walletId → 404.
- 2026-09-18: Added `jsonwebtoken`, `src/middleware/auth.js` (`requireAuth`), and dev-only `POST /auth/token`. Applied `requireAuth` to `GET /wallets/:userId` only — `POST /internal/wallets/:walletId/adjust` is intentionally still unauthenticated ("for now"). Verified via curl: no token → 401 missing token, garbage token → 401 invalid or expired token, minted token via `/auth/token` → 200 on the protected route, adjust endpoint confirmed still reachable without a token.
- 2026-09-18: Added `mongoose`, `transaction-service/src/db.js`, `src/models/Transaction.js`, `src/repositories/transactionRepo.js`. A local `mongod` was already running natively on `localhost:27017` (not something set up this session, not Docker) — connected directly, no provisioning needed (Mongo creates the `transaction_service` DB lazily on first write). Verified with a throwaway smoke-test script: create/complete/fail a transaction, `findByUser` matches on both wallet fields, unreachable Mongo → logs error + exits 1 before `app.listen`. Found and fixed a real race (see transaction-service section above) where the unique `idempotencyKey` index wasn't built yet when the first request landed.
- 2026-09-18: Switched transaction-service off the local `mongod` onto **MongoDB Atlas** (M0 free cluster `miniledger`, user set up their own account/cluster/DB user/network access). Updated `MONGO_URI` in `.env`/`.env.example`. Hit and fixed the Windows SRV-DNS resolver bug described above (`dns.setServers` workaround in `src/db.js`). Re-ran the full smoke test against Atlas after the fix — create/duplicate-reject/complete/findByUser all passed, same as against local Mongo.
- 2026-09-18: Added `axios`/`uuid` to transaction-service, `accountClient.js`, and `POST /transfer` (full account-service ↔ transaction-service money-movement flow). Two design conflicts found and resolved with the user before implementing (see transaction-service section above): account-service's `GET /internal/wallets/:userId` added for unauthenticated service-to-service lookups, and the debit/credit calls use derived idempotency sub-keys instead of one shared key. Verified end-to-end via curl: created two fresh users, funded the source wallet (200 INR), transferred 75 → `201`, confirmed both balances actually moved (125.00 / 75.00); unknown user → `404`; over-limit transfer → `502` with `metadata.reason: "insufficient funds"` and confirmed neither balance changed; same-user and amount-0 → `400`.
- 2026-09-18: Added `axios-retry` to `accountClient.js` (3 retries, exponential backoff, never retries 422) and `GET /transactions/:userId`. Also added `{ timestamps: true }` to the `Transaction` schema and switched `findByUser`'s sort from `_id` to `createdAt` — the task asked for "sorted by createdAt descending" but that field didn't exist yet; added it rather than leave the gap. Verified: a 422 (insufficient funds) failed in ~290ms (no retries); pointing `ACCOUNT_SERVICE_URL` at an unreachable port made the same kind of call take ~1656ms before failing (confirms retries + backoff are actually happening) and still failed cleanly with a JSON `500` rather than hanging. `GET /transactions/:userId` verified: newest-first ordering, `?limit`/`?offset` pagination actually slices results, invalid query params → `400`, unknown user → `404`.
- 2026-09-18: Added `jest` (dev dep) + `npm test` script to account-service, and `accountRepo.test.js` covering `adjustBalance` — fresh-key credit (asserts full call sequence incl. positive delta), replayed key (asserts cache hit AND that no `FOR UPDATE`/`UPDATE wallets`/`INSERT INTO idempotency_keys` calls happen), debit-exceeds-balance (422, asserts `ROLLBACK` with no write calls, negative delta), and a valid debit (asserts negative delta + full write sequence). Used plain `jest.fn()` objects mocking `pg.Pool` entirely rather than `jest-mock-extended` (offered as an alternative in the request) — this is plain JS with a small, fully-known mock surface, so a TS-interface auto-mocker wasn't worth the extra dependency. Sanity-checked the suite's own strictness by temporarily injecting a bug (an extra `UPDATE` on the insufficient-funds path) — the suite failed as expected, then reverted cleanly; `npm test` passes 4/4 with the container-free, DB-free mocked setup.
- 2026-09-18: Added `supertest` to both services, `app.js`/`index.js` split in both (see Structure above — pure refactor, no behavior change, confirmed by starting both normally afterward). account-service: `DATABASE_URL_TEST` (+ `miniledger_test` DB created in the same Postgres container), a Jest `globalSetup` that resets (`DROP SCHEMA public CASCADE`) and re-migrates that test DB fresh on every run (only ever targets `DATABASE_URL_TEST`, hard-fails if it's unset — never touches the real dev DB), plus `users.test.js`/`wallets.test.js` against the real app + real (test) Postgres. Verified: `npm test` run twice in a row both pass (proves the reset makes it repeatable — the migrations aren't idempotent on their own), and confirmed the real `miniledger` dev DB's tables/data were untouched afterward. transaction-service: installed `jest` (wasn't there before) + `supertest`, added `transfer.test.js` with `accountClient` fully mocked (`jest.mock`, no real HTTP/account-service involved) but real MongoDB Atlas — asserts a `completed` Transaction when the mocked `adjustWallet` resolves and a `failed` one when it rejects, verified the persisted doc's status directly via `Transaction.findById`, and confirmed cleanup left zero lingering test docs in Atlas afterward. Also had to pin `uuid` down from `^14` (ESM-only, broke under Jest's CJS `require`) to `^11` (current CJS-compatible major per npm's own deprecation notice) — a dependency-only fix, no logic change, surfaced only because this was the first time Jest had to load `transfer.js`.
- 2026-09-18: Documented AWS RDS / MongoDB Atlas M0 connection-string formats in both `.env.example` files, as commented alternatives alongside the existing local-dev defaults (not a default-value swap — an RDS/Atlas hostname as the *active* local-dev placeholder would break `npm run dev`/Jest, which are built around local Postgres/Mongo). Walked the user through actually provisioning RDS (db.t3.micro, 20GB, same VPC as future ECS, SG restricted to the ECS tasks' SG on 5432), Atlas M0 (DB user, network access), and running `migrations/001_init.sql`+`002_idempotency.sql` against the new RDS endpoint via a temporary my-IP-only SG rule — all external console/CLI steps I can't execute myself (no AWS/Atlas account access), delivered as a written walkthrough rather than as executed actions. **Not yet actually done**: no RDS instance or Atlas M0 cluster exists yet as of this entry — this was instructions only, nothing provisioned, `.env` files still point at the local Postgres container / the existing Atlas cluster set up earlier in the session.
- Postgres is running as a **Docker container** (not a native install): `docker run --name miniledger-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=miniledger -p 5432:5432 -d postgres:16`. `DATABASE_URL` in `account-service/.env(.example)` updated to `postgres://postgres:postgres@localhost:5432/miniledger` to match (was previously pointing at a nonexistent `account_service` db). Container must be running (`docker ps`) — if stopped, restart with `docker start miniledger-postgres`.
- `.env` placeholder values (other than `DATABASE_URL`, now real/working) are still dummy/local only (`JWT_SECRET=changeme`, etc.).
- Ran `git init` in the project root while testing `.gitignore` rules (repo wasn't git-initialized before); no commits made yet.

## Pending / not yet done
- `docker-compose.yml`'s `account-service`/`transaction-service` still get their real secrets (Atlas creds are *not* used here — `MONGO_URI` is overridden to the local `mongo` service, so that's fine) from each service's own `.env` via `env_file:` — anyone cloning the repo needs those `.env` files populated locally (from `.env.example`) before `docker compose up` will fully work, same as for `npm run dev`.
- Auth only guards `GET /wallets/:userId` — every other route, including everything under `/internal/` (both wallet lookup and adjust), is unauthenticated. That's fine while both services run on trusted localhost/same-network, but there's no real service-to-service auth (API key, mTLS, etc.) if this ever needs to be exposed more broadly.
- `POST /transfer` has no compensating-transaction logic — a debit-succeeds/credit-fails partial failure leaves the source wallet debited with no automatic reversal (see transaction-service section above).
- transaction-service now depends on MongoDB Atlas being reachable (internet + Atlas Network Access allowing this machine's IP) — no longer tied to the local `mongod` staying up, but also no longer works fully offline.
- No AWS or other cloud infra — and per explicit user decision, none is planned. `DEPLOYMENT.md` is a reference design only; don't suggest resuming the ECR/ECS work unless the user brings it up again.
- No CI. Automated test coverage now includes: account-service's `accountRepo.adjustBalance` (mocked `pg`), `POST /users` (success + 409), `GET /wallets/:userId` (200/404/401) against a real test Postgres DB; transaction-service's `POST /transfer` (completed + failed paths) with `accountClient` mocked but real Mongo. Still untested: account-service's `createUser`'s own unit behavior beyond the route test, `POST /internal/wallets/:walletId/adjust` and `GET /internal/wallets/:userId` as routes (only `adjustBalance` itself is unit-tested), `POST /auth/token`'s own behavior beyond using it as test fixture setup; transaction-service's `GET /transactions/:userId`, `transactionRepo` functions directly, and `accountClient` itself (only ever mocked, never tested against a real or fake HTTP server).

## Working agreement
- When a task requires the user to do external setup (e.g. install/configure MongoDB, Postgres, AWS resources, etc.), give explicit step-by-step instructions rather than assuming it's done.
