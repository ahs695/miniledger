PORT=4002

# MongoDB Atlas (M0 free tier) — get the real values from
# Atlas -> Database -> Connect -> Drivers
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-name>.<random-id>.mongodb.net/transaction_service?retryWrites=true&w=majority&appName=miniledger

JWT_SECRET=changeme
ACCOUNT_SERVICE_URL=http://localhost:4001
